node_modules/
coverage/
.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*
build/

# Source maps are regeneration artifacts; keep lib/ committed (git installs of
# this plugin run out of the box) but drop the large .map companions.
lib/**/*.map
